/// <reference path="math.d.ts" />

import geometry = require("./math/geometry");
import matrix = require("./math/matrix");

var result = {
    geometry: geometry,
    matrix: matrix
};

export = result;
